name: keda

type: go

up:
  - go:
      version: 1.19.5
      modules: true
  - homebrew:
      - golangci-lint

# `dev run` runs `dev build` and then `dev run-built` if these are both defined.
commands:
  build:
    run: make build
  test:
    run: make test

